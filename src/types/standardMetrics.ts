
// Stub file for backward compatibility
// Standard metrics functionality has been removed

export interface StandardMetric {
  id: string;
  name: string;
  category: string;
}

export interface CreateStandardMetricData {
  name: string;
  category: string;
}
